{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "strapi-hook-bookshelf",
      "settings": {
        "client": "postgres",
        "host": "/cloudsql/${process.env.INSTANCE_CONNECTION_NAME || 'guess-who-232508:europe-west1:guess-who-database'}",
        "port": "${process.env.DATABASE_PORT || 5432}",
        "database": "${process.env.DATABASE_NAME || 'postgres'}",
        "username": "${process.env.DATABASE_USERNAME || 'developer'}",
        "password": "${process.env.DATABASE_PASSWORD || 'cxJu3RK5QXzNuf9Y'}"
      },
      "options": {}
    }
  }
}
